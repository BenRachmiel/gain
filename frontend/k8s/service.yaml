apiVersion: v1
kind: Service
metadata:
  name: music-downloader-frontend
  namespace: navidrome
  labels:
    app: music-downloader-frontend
spec:
  type: ClusterIP
  selector:
    app: music-downloader-frontend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
