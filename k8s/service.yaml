apiVersion: v1
kind: Service
metadata:
  name: music-downloader
  namespace: navidrome
  labels:
    app: music-downloader
spec:
  type: ClusterIP
  selector:
    app: music-downloader
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
